# @waste-x/operations-core

Pure Waste X operational business rules shared by Web, Desktop and later Mobile.

Rules in this package must not import Next.js, React, Tauri, Expo, PostgreSQL, SQLite, AWS SDKs or other infrastructure-specific code.

The first extraction is intentionally small. Existing Daily Operations rules will be moved here incrementally so the current Web application remains stable while Desktop is introduced.
